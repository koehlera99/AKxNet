@model IEnumerable<Prime.Data.Entity.Squad>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SquadName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SquadName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SquadId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SquadId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SquadId })
        </td>
    </tr>
}

</table>
